# 渲染优化验证与移动端实践

优化不是“做了一项改动”，而是同一条件下，某个瓶颈指标得到稳定改善，同时画质、内存、加载和其他场景没有不可接受回退。

## 建立基线

记录：

- 目标设备和系统版本；
- 分辨率、画质、帧率目标；
- 场景和相机路径；
- CPU/GPU 帧时间；
- 内存和包体；
- 温度稳定后的持续性能；
- 截图或画质参考。

基线数据应保存原始 Capture/Profiler 文件，而不只抄一张表。

## 一次只验证一个主要假设

同时改合批、LOD、Shader 和纹理后，即使帧时间变快，也无法知道收益来自哪一项。

推荐流程：

1. 发现稳定瓶颈；
2. 提出成本模型；
3. 设计最小改动；
4. A/B 测试；
5. 检查画质和其他指标；
6. 在多场景、多设备回归；
7. 决定是否保留。

## 统计方式

不要只测一次。至少记录多个稳定区间，并关注：

- Median；
- P95/P99；
- 最坏帧；
- 标准差或波动范围；
- 加载、战斗峰值和场景切换。

提升 0.1 ms 但波动 1 ms 的结果不能证明有效。

## Tile-based GPU

多数移动 GPU 使用 Tile-based Rendering。它先把屏幕分 Tile，在片上 Tile Memory 中完成可见性和部分颜色/深度操作，最后再写回外部内存。

片上内存带宽高、功耗低。真正昂贵的是：

- Attachment Store/Load；
- 中途切换 Render Target；
- 需要把 Tile 内容写回再读取；
- 大量透明混合；
- 全屏 Pass；
- 超大 GBuffer 和多采样数据外存。

## TBDR 和 Immediate Mode

不同移动 GPU 的 Tile 架构并不完全相同。TBDR 可能先完成隐藏面排序，减少不可见片元；其他 Tile-based 方案主要利用片上缓存。

不能把“移动端都是 TBDR”当成统一规则。要查看目标 GPU 厂商文档和 Counter。

## Mobile Deferred

Deferred 在移动端是否合适，取决于 GBuffer 能否留在 Tile、是否使用 Subpass/Input Attachment、灯光数量和材质需求。

如果每个 Pass 都把 GBuffer 写回系统内存再读取，带宽会很高。移动 Deferred 往往使用紧凑 GBuffer、Framebuffer Fetch 或 Tile-local 技术。

## MSAA 在移动端

MSAA 增加样本存储和部分光栅成本，但在 Tile Memory 内完成时可能比多个后处理抗锯齿 Pass 更合适。Resolve 和 Store 到外部内存仍有成本。

具体选择要测试 Forward/Deferred、透明、分辨率和 GPU 架构。

## Overdraw 和透明

透明通常无法靠隐藏面消除，粒子覆盖会重复执行 Shader 和 Blend。

移动端优化：

- 收紧粒子 Quad；
- 控制屏幕面积和重叠，而非只控制数量；
- 远景半分辨率；
- 减少透明纹理采样和分支；
- 简化软粒子深度读取；
- 对 UI 大面积半透明特别警惕。

## 带宽优先思维

移动 GPU 常比桌面更受带宽和功耗限制。可以检查：

- Render Target 格式；
- 是否需要 Alpha；
- 是否可以 Half Resolution；
- 是否重复读写 Scene Color/Depth；
- 纹理压缩和 Mip；
- Temporary RT 生命周期；
- Store/Load Action；
- Buffer 是否连续访问。

减少 ALU 不一定加速一个带宽受限 Shader。适量计算替代纹理读取有时更好。

## 热和 DVFS

移动 CPU/GPU 会根据温度和功耗动态调整频率。刚启动的 60 FPS 不能代表持续性能。

测试应：

- 预热设备；
- 运行足够长时间；
- 记录温度和频率；
- 避免充电、后台应用和环境温度干扰；
- 比较相同热状态。

优化降低功耗后，即使冷机帧时间变化不大，也可能让持续频率更稳定。

## 设备矩阵

至少覆盖：

- 低端目标机；
- 主流目标机；
- 高端机；
- 不同 GPU 厂商；
- iOS/Android 或目标系统版本。

一种压缩格式、精度或 Compute Group Size 在所有设备上不一定同样合适。

## 画质与性能回归（Regression）

性能通过后检查：

- 阴影和 LOD Pop；
- 纹理清晰度和 Streaming；
- Normal/Gradient 压缩；
- TAA 拖影；
- 粒子密度；
- HDR、曝光和色带；
- UI 可读性。

可以使用固定相机截图差异，但动态效果还需要视频和人工观察。

## 优化记录模板不是正文模板

一次优化记录至少留下：

```text
场景/设备：
瓶颈证据：
修改内容：
修改前后指标：
画质和内存变化：
回归范围：
结论和适用边界：
```

这是实验记录，正式知识文章不需要统一套用。

## 相关主题

- [[知识库/14_性能分析与优化/帧时间、瓶颈与GPU成本]]
- [[知识库/14_性能分析与优化/Profiler、RenderDoc与单帧分析]]
- [[知识库/06_纹理技术/纹理采样、过滤、Mipmap与压缩]]
- [[知识库/13_引擎架构与资源系统/Draw Call、Batching与GPU Instancing]]

## 参考资料

- Arm, *Mali GPU Best Practices*.
- Qualcomm, *Adreno GPU Performance* guides.
- Apple, *Metal Best Practices Guide*.
- Android, *Performance Tuner* and *Android GPU Inspector* documentation.
