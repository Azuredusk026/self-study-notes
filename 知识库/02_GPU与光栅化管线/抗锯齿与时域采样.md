# 抗锯齿与时域采样

锯齿不是只发生在三角形边缘。几何覆盖、纹理、高光、阴影和透明测试都会产生高频信号。不同抗锯齿方法处理的信息不同。

## 超采样（SSAA）

用更高分辨率渲染，再过滤到目标分辨率。它同时改善几何、纹理和 Shader 锯齿，接近最直接的参考方案。

代价是几何、像素、带宽和内存都随采样数增加，实时项目很少全程使用。

## MSAA

MSAA 在每个像素保存多个 Coverage/Depth 样本，但通常只执行一次 Pixel Shader，再把结果写到覆盖样本。

它擅长处理三角形边缘，因为同一像素内多个样本可以表示部分覆盖。

它不能自动解决：

- Shader 内部的程序化硬边；
- 高频法线和高光；
- 纹理缩小；
- 没有使用 Alpha-to-Coverage 的 Alpha Test 边缘；
- 时域闪烁。

Deferred Rendering 使用多个 GBuffer 时，MSAA 会增加存储和解析复杂度，因此很多引擎改用时域方案。

### Sample Frequency 和 Resolve

MSAA 的 Coverage 和 Depth 按样本保存，Pixel Shader 默认可以按像素频率运行。三角形边缘的多个样本可能属于不同 Primitive，此时实现会根据覆盖拆分调用，或让 Shader 按样本频率执行。`centroid` 插值可避免插值位置落在当前 Primitive 覆盖之外。

Resolve 把多样本颜色转换成单样本颜色。普通线性颜色通常做算术平均：

```hlsl
Texture2DMS<float4, 4> MsaaColor : register(t0);

float4 Resolve4x(uint2 pixel)
{
    float4 sum = 0.0;
    [unroll]
    for (uint sampleIndex = 0; sampleIndex < 4; ++sampleIndex)
        sum += MsaaColor.Load(pixel, sampleIndex);
    return sum * 0.25;
}
```

实际 API 通常提供硬件 Resolve。手写代码适合需要自定义权重或检查样本值的场景。输入应该是线性颜色；在 sRGB 编码值上直接平均会得到错误亮度。

Depth、Stencil、Normal、Material ID 等数据没有统一的“平均”含义。Depth 可能按最近样本、最远样本或保留多样本形式处理，取决于反向 Z 和后续用途。Deferred 在几何边缘可能需要逐样本光照，或通过边缘检测只对复杂像素走特殊路径。

## FXAA

FXAA 是单帧后处理。它根据亮度或颜色变化检测边缘，再沿边缘方向平滑。

优点是便宜、容易接入。缺点是没有真实子像素覆盖信息，可能漏掉边缘或模糊文字、纹理细节。它也不能解决随时间变化的闪烁。

## 时域方案

时域抗锯齿把每像素的多个采样点分摊到连续多帧：每帧用略微不同的亚像素位置采样，再把历史结果重投影到当前帧累积。它用时间换取采样数。

相比空间方法，它的优势是能同时处理几何、纹理与着色锯齿，稳态等效采样数可以远超常见的 MSAA 档位，且成本只与输出分辨率相关、与场景复杂度无关。

代价是引入了一整套新问题：历史可能因遮挡变化而失效、因着色变化而陈旧，重采样误差会逐帧累积。它与渲染管线的耦合也远比空间方法深。

抖动、重投影、历史校验、样本累积的完整机制，以及鬼影、闪烁、模糊各类伪影的成因与对策，见 [[02_GPU与光栅化管线/TAA与时域超采样]]。

## 怎么选

| 方法 | 处理的锯齿 | 主要代价 | 适用 |
|---|---|---|---|
| SSAA | 全部来源 | 全流程按倍率增长 | 离线参考、截图 |
| MSAA | 几何边缘 | 随几何与深度复杂度变化 | 前向渲染、移动端 |
| FXAA | 图像上的边缘 | 极低 | 低端兜底、叠加使用 |
| TAA | 全部来源 | 与输出分辨率相关 | 延迟渲染、桌面与主机 |

延迟渲染中 MSAA 的多重采样 GBuffer 会抵消掉大部分收益，因此主流选择是时域方案。移动端的 Tile 架构上 MSAA 往往更便宜，前向渲染器中仍是合理选择。

这些方法并不互斥。常见组合是以时域方案为主，用空间方法处理它尚未收敛的区域。
## 验证方法

- 用高对比的斜边、细线与棋盘格纹理做基准，它们最容易暴露采样不足。
- 逐项关闭各抗锯齿方法对比，确认每一项实际解决了哪类锯齿。
- 检查 Alpha Test 边缘：MSAA 对它无效，需要 Alpha-to-Coverage 或其他手段。
- 检查程序化硬边与高频高光，这类着色锯齿不在 MSAA 的处理范围内。
- 在 sRGB 与线性两种颜色状态下各做一次 Resolve，确认没有在编码值上直接平均。
- 缓慢平移相机观察时域稳定性，单帧方法无法消除逐帧跳变。

## 相关主题

- [[02_GPU与光栅化管线/TAA与时域超采样]]
- [[01_数学与采样/信号、频率与噪声]]
- [[02_GPU与光栅化管线/光栅化、插值与深度模板]]
- [[06_纹理技术/纹理采样、过滤、Mipmap与压缩]]
- [[13_引擎架构与资源系统/Forward、Deferred与Clustered渲染]]
- [[14_性能分析与优化/渲染优化验证与移动端实践]]

## 参考资料

- Jorge Jimenez et al., *Filtering Approaches for Real-Time Anti-Aliasing*, SIGGRAPH Courses.
- Timothy Lottes, *FXAA*, NVIDIA.
- Microsoft Learn, *Multisample Antialiasing* and `Texture2DMS` documentation.
- LearnOpenGL, `src/4.advanced_opengl/11.1.anti_aliasing_msaa` and `11.2.anti_aliasing_offscreen`.