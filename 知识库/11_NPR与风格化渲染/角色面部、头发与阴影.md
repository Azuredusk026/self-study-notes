# 角色面部、头发与阴影

二次元角色最敏感的是脸、头发和阴影边界。直接使用几何法线做 Lambert，鼻梁和面颊会产生真实但不符合原画的阴影。角色方案通常用专用数据重建可控明暗。

## 面部为什么难

人脸几何有鼻翼、眼窝、嘴角等高频曲率。真实光照会产生细碎阴影，而二维原画常只保留一块连续的脸部暗面。

常用组合：

- 编辑过的 Face Normal；
- Face SDF 控制大阴影边界；
- AO/Mask 固定眼窝、鼻底等细节；
- Ramp 控制亮暗颜色；
- 实时 Shadow Map 处理头发投影和外部遮挡。

这些数据解决不同问题，不应互相替代。

## Face SDF 的含义

Face SDF 通常把一组预设计脸部阴影边界编码到 UV Texture。它不一定是真正以世界单位保存的严格 Signed Distance；很多项目保存的是可阈值比较的灰度场。

运行时把主光方向变到 Head Local Space。只考虑脸部水平平面上的方向，得到光从左或右照入的角度，再用该角度选择阈值：

$$
\mathbf{l}_h=\operatorname{normalize}(M_{world\rightarrow head}\mathbf{L})
$$

$$
a=\frac{\operatorname{atan2}(l_{h,x},l_{h,z})}{\pi}
$$

具体 Forward Axis、符号、UV 左右和阈值映射取决于项目约定。不能复制公式后假设方向自动正确。

## 左右脸与翻转

很多 Face SDF 只烘焙半边或在一张纹理中编码左右两组边界。运行时根据 $l_{h,x}$ 选择原 UV 或镜像 UV。

必须验证：

- 模型 Head Forward 是 +Z 还是其他轴；
- Directional Light 变量表示“光传播方向”还是“指向光源”；
- UV 原点与左右镜像方式；
- 贴图灰度是“距离亮区”还是“距离暗区”；
- 光从正前/正后时如何退化。

最简单的校验是固定头部，绕 Y 轴旋转一圈主光，并输出光方向、采样值和最终阈值。

## SDF 阈值与软边

如果采样值为 $d$、角度映射阈值为 $t$，可使用：

$$
w=\operatorname{smoothstep}(t-\epsilon,t+\epsilon,d)
$$

$w$ 在亮暗层间插值。$\epsilon$ 可结合 `fwidth(d-t)` 做屏幕空间抗锯齿。

SDF 贴图需要足够 Padding 和合适压缩。强有损压缩会让平滑距离场出现块状边界。Mipmap 则可能改变阈值，需要在不同距离检查。

## 头发投脸阴影

Face SDF 只描述脸部自阴影，不能准确处理刘海。头发投影可以使用：

- 普通 Shadow Map；
- 单独 Hair Shadow Pass，把头发深度/Mask 投到 Face；
- Screen-space Offset Shadow；
- 美术绘制的固定 Hair Shadow Mask。

Shadow Map 物理关系较完整，但 Bias、分辨率和 PCF 会影响细线。Screen-space Offset 便宜且可控，却会随 FOV、头部旋转和遮挡穿帮。

专用 Hair Shadow Pass 可以只让头发写入角色局部 Shadow Mask，再由脸材质采样。需要用 Stencil/ID 防止投到场景或其他角色，并处理透明发片与 Alpha Clip。

## 角色 Shadow Map

PCF 对多个深度比较结果求平均，得到柔和可见性。PCSS 先查找 Blocker，再根据接收面与遮挡物距离估计 Penumbra，最后改变 PCF Kernel。

角色 NPR 不一定追求物理软阴影。常用控制：

- 固定或量化 Filter Radius，保持动画片色块；
- 给脸、头发和身体不同 Bias；
- 用 Shadow Ramp 重映射可见性；
- 限制自阴影区域；
- 主光使用高质量阴影，附加光不产生复杂色块。

Bias 过大造成 Peter Panning，过小产生 Acne。Normal Bias 还会让细脸和发片轮廓偏移，必须近距离检查。

## 头发的切线方向

头发高光沿发丝方向拉伸。每个顶点需要稳定的 Tangent/Bitangent，或通过 UV、Flow Map 重建方向。

发片 UV 一般让 V 沿发根到发梢。若不同发片 UV 方向不一致，高光会突然反向。导入验证应显示 Tangent 和 Flow。

几何切线过于规则时，高光会像塑料条。可用 Noise/Shift Texture 对切线方向或高光位置做低幅度扰动，但不能破坏整体发流。

## Kajiya-Kay 头发高光

Kajiya-Kay 用发丝 Tangent $\mathbf{T}$ 与半角向量 $\mathbf{H}$ 近似纵向高光。一个常见形式是：

$$
\sin\theta=\sqrt{1-(\mathbf{T}\cdot\mathbf{H})^2}
$$

$$
S=\left(\sin\theta\right)^p
$$

$p$ 控制高光宽度。为了形成偏移的主/次高光，可沿 Normal 对 Tangent 做 Shift，再分别计算两个 Lobe：

$$
\mathbf{T}'=\operatorname{normalize}(\mathbf{T}+s\mathbf{N})
$$

两个 Lobe 使用不同 Shift、Exponent、Color 和 Mask。Toon Hair 常再对 $S$ 做 Ramp 或阈值，使高光形成清晰色带。

这是经验模型，不是完整毛发散射。近景写实发丝可使用 Marschner 等模型；二次元发片更重视可控形状与稳定性。

## 高光 Mask 与视角稳定

头发高光通常还乘：

- Root-to-tip Gradient；
- Strand/Noise Mask；
- Artist-painted Specular Mask；
- Shadow Visibility；
- $N\cdot L$ 或自定义光照范围；
- Camera-facing Mask。

完全粘在 View Direction 上会像屏幕贴纸，完全跟随光源又可能在镜头中消失。可在 Light-based 与 View-based 坐标间混合，具体比例由项目风格决定。

## 面部法线与眼睛

Face Normal 常经过传递或手工编辑，让面颊形成更完整的明暗面。修改后必须保留眼窝、鼻翼等必要结构，并检查与 Normal Map 的叠加。

眼球通常需要独立材质层：

- Iris/Pupil 可能在切线或局部空间做 Parallax；
- Cornea 高光与虹膜颜色分开；
- 眼白避免纯白并接收受控阴影；
- 上眼睑阴影和睫毛用专用 Mask；
- Look-at 时左右眼方向、汇聚距离和高光应稳定。

## 调试与验证

- 输出 Head Local Light Direction、Face SDF UV、左右翻转标记和阈值。
- 主光绕头一圈，检查正面、侧面和背面连续性。
- 分别关闭 Face SDF、Hair Shadow、实时 Shadow 和 Face Normal 修正。
- 显示 Hair Tangent、Shift 与两个 Specular Lobe。
- 检查头部动画、非均匀缩放、镜像角色和不同模型 Forward Axis。
- 在 TAA、MSAA、不同阴影分辨率和角色距离下做回归。

## 相关主题

- [[知识库/04_光照模型与PBR/法线贴图、切线空间与IBL]]
- [[知识库/05_光照阴影与GI/阴影贴图、PCF与PCSS]]
- [[知识库/11_NPR与风格化渲染/NPR材质与分层光照]]
- [[知识库/11_NPR与风格化渲染/描边、Billboard与场景风格化]]

## 参考资料

- Kajiya and Kay, *Rendering Fur with Three Dimensional Textures*.
- Marschner et al., *Light Scattering from Human Hair Fibers*.
- Arc System Works and miHoYo technical presentations on anime character rendering.
